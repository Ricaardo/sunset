version: '3.10'

services:
  sunset:
    build: .
    container_name: sunset
    restart: unless-stopped
    networks:
      - sunset-network
    ports:
      - "8080:8080"

networks:
  sunset-network:
    driver: bridge
